version: '3.8'

services:
  api:
    build: .
    container_name: fastapi-clean-api
    ports:
      - "8001:8000"
    volumes:
      - .:/app
    environment:
      - MONGO_URI=${MONGO_URI}
      - POSTGRES_URI=${POSTGRES_URI}
    env_file:
      - .env
